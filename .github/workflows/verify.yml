name: Verify repository

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run static and secret checks
        run: bash scripts/verify_release.sh

