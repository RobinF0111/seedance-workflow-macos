# 安全说明与漏洞报告

## 支持范围

当前接受 V4.3.5 的安全问题报告，重点包括：

- API Key 的保存、显示、清除或意外泄露；
- 本地服务端口被其他应用访问；
- 用户图片、剧本、历史记录或输出被非预期发送；
- 路径遍历、任意文件读取或写入；
- 应用包完整性和依赖投毒风险。

## 报告方式

请不要在公开 Issue 中提交真实 API Key、私有剧本、客户素材或可直接利用的敏感细节。可先通过仓库的 Security Advisory 私密报告功能联系维护者；若该功能不可用，请仅创建不含敏感细节的 Issue，请求维护者提供私密沟通渠道。

报告建议包含：

- 受影响版本与 macOS 版本；
- 可复现步骤；
- 预期行为与实际行为；
- 影响范围；
- 已脱敏的截图或日志。

## 用户安全建议

- 为本应用创建独立、可随时撤销的 OpenAI API Key；
- 不在输入、截图、日志或 GitHub Issue 中粘贴 API Key；
- 定期检查 API 用量并设置账户侧预算或告警；
- 不要运行来源不明或校验值不一致的软件包；
- 怀疑泄露时，立即在 OpenAI 后台撤销旧 Key。

## 已知信任边界

当前仓库提供的是已构建 macOS 应用，核心本地服务位于编译产物 `server.code` 中，尚未提供完整的可复现构建链。当前应用为 ad-hoc 签名，尚未完成 Apple Developer ID 签名与 notarization。在这两项完成前，发布说明必须明确披露这一状态，不得宣称已通过 Apple 公证。

