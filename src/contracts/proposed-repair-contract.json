{
  "type": "json_schema",
  "name": "seedance_owner_repair_v43",
  "strict": true,
  "schema": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "enum": ["sd", "style"]
      },
      "patches": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "object",
          "properties": {
            "section": {
              "type": "string",
              "enum": ["style", "light", "references", "scene", "shots", "performance", "negative"]
            },
            "shotId": {"type": "string", "minLength": 1},
            "fieldPath": {"type": "string", "minLength": 1},
            "replacementValue": {"type": "string", "minLength": 1}
          },
          "required": ["section", "shotId", "fieldPath", "replacementValue"],
          "additionalProperties": false
        }
      }
    },
    "required": ["owner", "patches"],
    "additionalProperties": false
  }
}
