# 隐私说明

生效日期：2026-08-31

Seedance Workflow 是运行在用户 Mac 上的提示词创作工具。本说明描述 V4.3.5 处理的数据类型、用途和用户可执行的控制操作。

## 处理的数据

当用户主动发起生成或修改任务时，应用会处理：

- 用户输入的剧情、分镜、提示词和修改要求；
- 用户主动上传或粘贴的图片；
- 用户填写的 OpenAI API Key；
- API 返回的生成结果、模型信息和 Token 用量；
- 用户选择启用本地历史后保存的输入、图片、输出和修改记录。

## 数据发送范围

为完成 AI 生成，应用会将本次任务所需的文字和图片发送至 OpenAI API。应用不会因为用户仅打开界面或浏览本地历史而主动发送历史项目。

具体 API 数据处理规则、保留期限和地区安排由用户所使用的 OpenAI 账户及其适用条款决定。请勿提交无权处理的个人信息、商业秘密或受限制素材。

## 本地保存

- API Key 通过应用设置界面配置。系统安全存储可用时，由当前 macOS 账户加密保存；不可用时，应用会提示仅在本次运行中保留。
- 历史记录只在用户主动选择目录后启用，保存位置由用户决定。
- 历史记录可能包含输入文字、图片、输出和修改记录，但不包含 API Key。

## 用户控制

用户可以：

- 在 API 设置中点击“清除当前密钥”，撤销应用对本机已保存 Key 的使用；
- 在 OpenAI 后台撤销或轮换 API Key；
- 不启用本地历史记录；
- 删除自己选择的历史记录文件夹；
- 在发送前移除图片或删除输入内容。

## 当前产品边界

V4.3.5 不提供云端账号、团队共享空间或开发者运营的项目数据库。应用本身不直接生成或托管视频。

## 变更

隐私处理方式发生实质变化时，应同步更新本文件与版本变更记录。

